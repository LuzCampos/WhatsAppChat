import 'package:flutter/material.dart';

class CamaraTab extends StatelessWidget {
  const CamaraTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(Icons.camera)
    );
  }
}